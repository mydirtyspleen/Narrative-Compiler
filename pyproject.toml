[project]
name = "adm-api"
version = "0.1.0"
description = "Deterministic real-time narrative infrastructure layer"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.136.1",
    "pydantic>=2.13.4",
    "python-multipart>=0.0.29",
    "uvicorn[standard]>=0.47.0",
    "websockets>=16.0",
]
